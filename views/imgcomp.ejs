<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Image Compression</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background-image: url('img/background.jpg');
            background-size: cover;
            background-position: center;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .form-container {
            max-width: 400px;
            margin: 0 auto;
            padding: 20px;
            background-color: rgba(255, 255, 255, 0.8);
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        h2 {
            margin-bottom: 20px;
            color: #333;
        }

        p {
            margin-bottom: 20px;
            line-height: 1.5;
            color: #666;
            font-size: 0.8em;
        }

        .form-group {
            margin-bottom: 20px;
            text-align: left;
        }

        label {
            display: block;
            margin-bottom: 5px;
            color: #333;
        }

        input[type="file"] {
            display: block;
            width: 100%;
            padding: 10px;
            margin-top: 5px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
        }

        input[type="range"] {
            width: 100%;
        }

        output {
            display: inline-block;
            font-size: 0.8em;
            margin-left: 5px;
            color: #666;
        }

        .submit-btn {
            display: inline-block;
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .submit-btn:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="form-container">
        <h2>Image Compression</h2>
        <p>Upload an image and select compression quality:</p>
        <form action="/compress-image" method="post" enctype="multipart/form-data">
            <div class="form-group">
                <label for="image">Upload Image:</label>
                <input type="file" id="image" name="image" accept="image/*" required>
            </div>
            <div class="form-group">
                <label for="compression">Compression Quality:</label>
                <input type="range" id="compression" name="compression" min="10" max="300" value="50" step="1">
                <output for="compression">50</output>
            </div>
            <button type="submit" class="submit-btn">Compress Image</button>
        </form>
    </div>

    <script>
        const compressionInput = document.getElementById('compression');
        const compressionOutput = document.querySelector('output[for="compression"]');

        compressionInput.addEventListener('input', function() {
            compressionOutput.value = this.value;
        });
    </script>
</body>
</html>
